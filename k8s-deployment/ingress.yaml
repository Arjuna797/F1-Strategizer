apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: f1strategizer-2401076-ingress
  namespace: "2401076"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
    - host: f1strategizer.imcc.com
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: f1strategizer-2401076
            port:
              number: 80
